/** Route: /hr — human resources workspace (placeholder). */
import { DepartmentScreen } from '@/components/horizon/department-screen';

export default function HrScreen() {
  return (
    <DepartmentScreen
      title="HR management"
      description="Manage employees, payroll, and team operations."
    />
  );
}
