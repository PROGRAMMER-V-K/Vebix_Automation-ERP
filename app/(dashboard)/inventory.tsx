/** Route: /inventory — inventory workspace (placeholder). */
import { DepartmentScreen } from '@/components/horizon/department-screen';

export default function InventoryScreen() {
  return (
    <DepartmentScreen
      title="Inventory management"
      description="Track stock, products, and warehouse operations."
    />
  );
}
