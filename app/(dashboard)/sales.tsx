/** Route: /sales — sales workspace (placeholder). */
import { DepartmentScreen } from '@/components/horizon/department-screen';

export default function SalesScreen() {
  return (
    <DepartmentScreen
      title="Sales management"
      description="Manage orders, customers, and sales pipelines."
    />
  );
}
