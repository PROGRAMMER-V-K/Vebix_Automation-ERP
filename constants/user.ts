/**
 * Default user profile constants.
 * Attendance and UI use Firebase Auth uid when signed in; these are fallbacks for display.
 */
export const CURRENT_USER_ID = 'isaac';

export const CURRENT_USER = {
  id: CURRENT_USER_ID,
  name: 'Isaac',
  email: 'isaac@example.com',
};
